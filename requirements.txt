# MyClaw 核心依赖
fastapi>=0.136.1
uvicorn[standard]>=0.46.0
pydantic>=2.13.4
httpx>=0.28.1
requests>=2.31.0
python-multipart>=0.0.18
numpy>=1.24.0

# 桌面应用打包
pywebview>=5.4
pyinstaller>=6.15.0

# 桌面自动化（通用，Windows 和 macOS 均需要）
pyautogui>=0.9.54
Pillow>=11.0.0
pyperclip>=1.9.0

# 桌面自动化（Windows 专属）
pywin32>=308; platform_system == "Windows"
pywinauto>=0.6.8; platform_system == "Windows"
uiautomation>=2.0.20; platform_system == "Windows"

# 桌面自动化（macOS 专属）
pyobjc-framework-Quartz>=12.0; platform_system == "Darwin"
pyobjc-framework-Cocoa>=12.0; platform_system == "Darwin"
pyobjc-framework-ApplicationServices>=12.0; platform_system == "Darwin"